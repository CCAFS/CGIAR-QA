import { FilterByRolePipe } from './filter-by-role.pipe';

describe('FilterByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
